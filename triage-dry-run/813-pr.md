# mvdan/sh #813 — BinaryNextLine for test/arith expressions

**Status:** INVESTIGATED — fix known
**Effort:** ~35 lines (20-30 printer + 15 test)
**Viability:** STRONG (8 external merges in 3 months, solo maintainer welcoming)
**Hypothesis:** H2/H5

## Fix

In `printer.go`: add `binNextLine` handling to `testExprSameLine` (for `BinaryTest`/`AndTest`/`OrTest`) and `arithmExprRecurse` (for `BinaryArithm`). Mirror the existing `BinaryCmd` logic at line 1236.

## PR plan

- Title: `printer: support -bn flag for test and arithmetic expressions`
- Body: Reference #813. Maintainer said "it would probably make sense to change the feature."
- Add test cases to `TestPrintBinaryNextLine` in `printer_test.go` (line 795).
- No competing PRs.

## Quality gates

- [x] Staleness check — issue OPEN, 0 competing PRs (2026-05-09)
- [ ] Test gate — needs fix branch
- [ ] Gemini volley — needs diff
- [x] Voice crosscheck — PASSED. Gemini flagged Entry 1 (real, disclosed Cursor use) and Entry 4 (real human). Called ours "deeply contextual" with "exact AST/function references."

## Blocked by

Needs code investigation to confirm exact lines, then ready.
