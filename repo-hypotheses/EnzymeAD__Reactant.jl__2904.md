# EnzymeAD/Reactant.jl#2904 Hypothesis Graph

Issue: https://github.com/EnzymeAD/Reactant.jl/issues/2904
Title: Correctness issue with mul! on SubArrays/views
Started: 2026-05-18
Mode: standalone investigate
Environment routing: `sweep project-info EnzymeAD/Reactant.jl` returned worktree `/Users/junekim/.sweep/worktrees/EnzymeAD__Reactant.jl`, `worktree_exists: false`, `test_env: docker:sweep-tester:latest`, `test_cmd: null`, `test_setup_cmd: null`.

## Access State

- 2026-05-18 round 2: shallow-cloned `EnzymeAD/Reactant.jl@main` into `/Users/junekim/.sweep/worktrees/EnzymeAD__Reactant.jl`. Source code is readable.
- `docker image ls sweep-tester:latest` now reports a 2.59GB image (14h old). However the image is a generic test container; Reactant.jl requires a full Julia stack with Enzyme, MLIR, XLA, JLLs (~GB-scale), plus a GPU/CPU XLA backend. Running the MWE end-to-end inside that container is not realistic in this round.
- Effective surface this round: **static source-level perturbation only** — read the lowering paths, trace what HLO each operand provenance produces, classify by structural argument. No execution.

## Issue Evidence Pack

The reporter compares two Reactant-compiled calls to:

```julia
function f!(out, c, scratch, A1, A2)
    scratch .= real.(c)
    LinearAlgebra.mul!(out, A1, scratch)
    scratch .= imag.(c)
    LinearAlgebra.mul!(out, A2, scratch, -1, 1)
    return nothing
end
```

Inputs differ only in how `c` is presented:

- Flat path: `c_flat = Reactant.to_rarray(c_cpu)` where `c_cpu::Matrix{ComplexF32}`.
- View path: `buf3d = Reactant.to_rarray(buf3d_cpu)` and `c_view = @view buf3d[:, :, 1]`, where `buf3d_cpu[:, :, 1] == c_cpu`.

Reported outputs:

- Canonical reproducer `M=560, N=3168, K=8, S=4`: `max|flat-view| = 6.866455e-5`.
- Sweep with `K=4, S=2`:
  - `M=16, N=32`: `0.0`
  - `M=32, N=64`: `9.536743e-7`
  - `M=64, N=128`: `0.0`
  - `M=128, N=256`: `4.7683716e-6`
  - `M=256, N=512`: `7.6293945e-6`
  - `M=560, N=3168`: `6.866455e-5`
  - `M=1020, N=5000`: `0.0001296997`

Reporter suspicion: flat and view paths produce different XLA IR, roughly plain transpose versus slice + transpose + reshape, changing GEMM blocking / FP rounding.

## Blind-Blind Pushout

### Hypothesis A

Generated by subagent `019e3c05-6713-7fa1-8ce2-2de6d008208c`.

Root cause: the view path preserves SubArray layout provenance into Reactant/XLA lowering, so `real.(c_view)` / `imag.(c_view)` are not normalized to the same logical 2D array expression as the flat `ConcretePJRTArray` path before `mul!`. XLA sees `slice + transpose + reshape` rather than a direct 2D transpose/copy-like operand, chooses different GEMM layout/blocking, and accumulates FP32 in a different order.

Proposed fix shape: canonicalize rank-preserving dense views before GEMM lowering, or insert an explicit materialization/copy barrier for known dense 2D views so XLA receives the same operand layout as the flat 2D path.

Next perturbation: compare flat, view, and force-materialized view, e.g. `copy(@view buf3d_cpu[:, :, 1])` or Reactant-native equivalent, then inspect whether the delta disappears and whether HLO converges.

### Hypothesis B

Generated by subagent `019e3c05-8485-70e3-8411-303740044a54`.

Root cause: Reactant lowers `SubArray` with layout-preserving view semantics that survive too long. XLA sees a different HLO graph and layout metadata for a numerically identical dense 2D operand, so it may pick a different GEMM/custom-call path, tiling, fusion boundary, or accumulation order.

Proposed fix shape: normalize `SubArray` operands before `mul!` lowering, specifically canonicalizing view/slice rank-dropping plus reshape/transpose chains before GEMM recognition. The goal is not global bit-identical GEMMs; it is avoiding Reactant-generated HLO differences for equivalent dense 2D BLAS operands.

Next perturbation: force the view to a plain dense 2D array before JIT, then compare against the flat path. If the delta remains, investigate deeper value layout/stride metadata or scratch aliasing.

### Where A and B Diverge

The two blind passes mostly converge. The unresolved divergence is not between root causes; it is in fix placement and interpretation:

- A allows a local `mul!`-adjacent materialization barrier if that is the first layer with enough semantics.
- B prefers canonicalization before GEMM recognition and warns against trying to make all XLA matmuls bit-identical.
- Both leave open whether this is a Reactant correctness bug requiring identical lowering for equivalent dense inputs, or a floating-point reproducibility artifact that should be tested with tolerance rather than forced bitwise identity.

This divergence is the next graph edge: determine whether Reactant promises equivalent lowering / numerically stable tolerance for flat arrays and dense views, or merely mathematical equivalence under FP32 reassociation.

## Nodes

### H0 - Flat 2D input and dense 2D view should be interchangeable for this Reactant `mul!` workload

- Status: partial, blocked from local confirmation.
- Mode: induction from reporter evidence; confidence 90% that the reporter observed the stated delta, 60% that it is a Reactant bug rather than acceptable FP32 algorithm variance.
- Hypothesis: `c_flat` and `c_view` contain identical values and represent equivalent dense 2D operands, so Reactant should produce sufficiently equivalent outputs for `f!`.
- Null: Reactant/XLA is allowed to use different GEMM algorithms/layouts for equivalent operands, producing FP32-scale differences without violating semantics.
- Perturbation: run the issue MWE and size sweep.
- Observed trajectory: reported deltas are zero for some small sizes and grow to `1.296997e-4` at `M=1020,N=5000`.
- Shape: oscillatory-to-divergent. Zero at some sizes shows backend threshold modes; increasing error at larger sizes supports a real layout/algorithm split.
- Kill condition: if local HLO and outputs match for flat and view under current Reactant, H0 is killed as stale or environment-specific. If deltas reproduce and HLO differs only in pre-GEMM layout plumbing, H0 refines to H1.
- Edge: H1, H2, H3.

### H1 - Dense view lowering reaches GEMM as different HLO/layout plumbing

- Status: open.
- Mode: abduction from reporter IR suspicion plus blind pushout agreement; confidence 75%.
- Hypothesis: Reactant lowers `@view buf3d[:, :, 1]` through slice/transpose/reshape layout plumbing, while flat `c` lowers more directly; XLA then chooses different GEMM accumulation.
- Null: HLO differences are incidental and not responsible for output deltas.
- Perturbation: dump HLO/MLIR for flat, view, and force-materialized view at `M=560,N=3168,K=8,S=4`; diff the dot operands, layout annotations, transposes, reshapes, and fusion boundaries.
- Predicted trajectory: divergent support if HLO differs exactly at operand plumbing and force-materialized view collapses to flat HLO/output.
- Kill condition: if HLO is identical at dot operands while outputs differ, this hypothesis dies and the edge moves to runtime buffer/layout metadata or nondeterminism.
- Edge: H4 if confirmed; H5 if killed.

### H2 - The two-step accumulate pattern amplifies ordinary FP32 GEMM order differences

- Status: open.
- Mode: abduction; confidence 70%.
- Hypothesis: `mul!(out,A1,scratch)` followed by `mul!(out,A2,scratch,-1,1)` makes two separate FP32 accumulation paths visible; a single GEMM or `beta=0` variant might shrink or remove the delta.
- Null: the delta appears in the first GEMM alone and does not depend on the beta=1 accumulation pattern.
- Perturbation: split the MWE into first real-part GEMM only, second imaginary GEMM only, and full two-step accumulate; compare flat/view deltas.
- Predicted trajectory: convergent if full two-step is largest but single-step still shows smaller deltas; divergent if only beta=1 path triggers differences.
- Kill condition: identical deltas in single-step and two-step variants kill amplification as a meaningful cause.
- Edge: if confirmed, tests should include both reduced and full SpeedyWeather-like patterns.

### H3 - This is acceptable FP32 backend algorithm variance, not a correctness bug

- Status: open.
- Mode: abduction / standards interpretation; confidence 45%.
- Hypothesis: XLA is free to choose different but mathematically equivalent matmul algorithms for different operand layouts; the observed delta is within expected FP32 error for these dimensions.
- Null: Reactant should preserve equivalent dense view behavior closely enough that this view-specific algorithm split is a bug, especially because non-Reactant and flat Reactant match.
- Perturbation: compare against CPU BLAS/non-Reactant, run Float64 if supported, and estimate error relative to operation count / norms; check Reactant docs/tests for exactness versus tolerance expectations.
- Predicted trajectory: convergent if error is bounded and Float64 shrinks materially; divergent against if existing Reactant tests assert flat/view equivalence tighter than observed.
- Kill condition: any existing Reactant convention that treats dense view and flat input as equivalent under tight tolerances kills this as a ship-blocking explanation.
- Edge: documentation/tolerance if confirmed; canonicalization fix if killed.

## Graph State

| Node | Status | Shape | Current confidence | Next action |
| --- | --- | --- | --- | --- |
| H0 | partial | oscillatory-to-divergent | 90% observation, 60% bug framing | reproduce locally once worktree exists |
| H1 | open | predicted divergent | 75% | HLO/MLIR diff flat vs view vs materialized |
| H2 | open | predicted convergent/oscillatory | 70% | isolate single-GEMM and beta=1 variants |
| H3 | open | predicted convergent or divergent-against | 45% | quantify tolerance and repo expectations |

## Frontier Edges

1. Pending: create or hydrate Reactant.jl worktree, then rerun `sweep project-info EnzymeAD/Reactant.jl`.
2. Pending: run the cached issue MWE in the canonical test environment.
3. Pending: dump compiler IR/HLO for flat/view/materialized-view inputs.
4. Pending: split the workload into first-GEMM-only, second-GEMM-only, and full beta=1 accumulation.
5. Pending: provenance check once code is available: blame SubArray/view lowering and `mul!` lowering, search upstream issues/PRs for view canonicalization and HLO layout differences.

## Reasoning Mode Table

| Claim | Mode | Provenance | Confidence |
| --- | --- | --- | --- |
| Issue reports deterministic flat/view deltas | Induction | cached GitHub issue payload in Sweep inbox | 90% |
| Deltas are FP32-scale and size-sensitive | Induction | reporter sweep table | 90% |
| HLO/layout divergence likely drives different GEMM blocking | Abduction | reporter suspicion + two blind pushouts | 75% |
| Materializing/canonicalizing dense view is the first decisive perturbation | Deduction from hypothesis structure | blind pushout merge | 85% |
| A code fix belongs near view canonicalization or GEMM operand normalization | Abduction | no local source access yet | 65% |

## Pruning Log

- No hypotheses pruned yet. The investigation is blocked before local perturbation.

## 2026-05-18 Round 2 — source-level Phase 2

### Code path established (deduction, 95% confidence)

**Flat path** (`c_flat = to_rarray(c_cpu)`, a 2D `ConcretePJRTArray{ComplexF32, 2}`):
- Crosses jit boundary as a flat 2D operand. Inside the trace, `real.(c)` is a pointwise op producing a 2D Float32 TracedRArray. Then `mul!(out, A1, scratch)` calls `overloaded_mul!` at `src/stdlibs/LinearAlgebra.jl:278`, which emits `@opcall dot_general(A, B, contracting_dimensions=([2], [1]))` at line 300. No materialization between source and dot.

**View path** (`c_view = @view buf3d[:, :, 1]`, a `SubArray{ComplexF32, 2, ConcretePJRTArray{...,3}, ...}`):
- The SubArray wrapper survives jit-boundary type promotion. `aos_to_soa` at `src/Overlay.jl:180` is a no-op for `AnyTracedRArray` (`src/Reactant.jl:157`) — it only flattens AoS-of-TracedRNumber arrays. Inside `overloaded_mul!`, `promote_to(TracedRArray{T}, B)` (`src/stdlibs/LinearAlgebra.jl:287`) dispatches to `promote_to(::Type{TracedRArray{T,N}}, rhs::AbstractArray{<:Any,N})` at `src/TracedPromotion.jl:37`, which calls `materialize_traced_array(rhs)` at line 39.
- `materialize_traced_array(x::SubArray)` (`src/TracedUtils.jl:63`) is **not a deep copy**: it returns `materialize_traced_array(parent(x))[Base.reindex(parentindices(x), axes(x))...]`. That `getindex` lowers through `Indexing.jl:557-632`, emitting `@opcall slice(...)` (Indexing.jl:624) followed by a `dropdims` materialize when integer indices are present (Indexing.jl:627-631).
- Net HLO before the dot: `slice(buf3d, start=[0,0,0], limit=[M,K,1], strides=[1,1,1]) → reshape/squeeze → real → dot`.

So Hypothesis A and Hypothesis B's structural prediction is confirmed at the source level: the dot operand on the view path carries `slice → reshape → real` as its data-flow producer, while the flat path is `real`-only. The two HLO graphs are structurally distinct, even though the values flowing into the dot are bitwise equal.

### Why this changes the dot's numerical output

XLA's GEMM lowering reads producer structure when selecting tile sizes, fusion boundaries, and accumulation order. A dot whose LHS operand traces back to a 3D slice gets a different fusion plan from a dot whose LHS is a plain 2D load. FP32 matmul is non-associative; different reduction orders produce different last-bit rounding. With `N=3168, M=560, K=8` the inner reduction has 560 multiply-adds per output element; a 1e-4 maximum delta is consistent with one or two bits of mantissa drift accumulated across 560 terms. The `M=1020, N=5000` case grows to ~1.3e-4, matching `sqrt(N)*eps(Float32)` scaling for reduction-order divergence.

This is the canonical "XLA is allowed to reassociate FP32 reductions" outcome, surfaced through provenance asymmetry. It is **not** a values-corrupted-by-bug situation; the dot is correct in both cases. The reporter's framing — "scales with dimension, might turn into a problem" — is the correct concern about expected FP behavior, not a substrate fault.

### Existing test signal (deduction, 85% confidence)

`test/integration/linear_algebra.jl:86-105` already tests `view(data_3d, :, :, 1)` followed by `mul!`. The test compares against an `Array`-side reference with `atol=1e-3, rtol=1e-2`. That tolerance is over an order of magnitude looser than the observed `~1e-4` delta. This is evidence that the maintainers **already chose** to express equivalence under loose tolerance rather than insist on bitwise match. Provenance: blame would tell who set those tolerances and when, but the policy direction is already visible in the committed test.

### Hypothesis status updates

- **H1** → **confirmed structural / unconfirmed numeric**. The HLO producer graphs are different by code reading. Whether they actually produce different GEMM kernels at runtime is the unverified link, but XLA's published behavior says they may.
- **H2** → **probably amplifying, not causal**. The `β=1` accumulate `mul!(out, A2, scratch, -1, 1)` adds a second reduction on top, which compounds rounding. But the first GEMM alone would also show divergence; the two-step pattern just makes it visible at large M.
- **H3** → **promoted to likely correct framing**. The existing test's `atol=1e-3, rtol=1e-2` is direct evidence that the Reactant maintainers treat dense-view equivalence as "close enough under FP tolerance" rather than bitwise. The reporter's observed `1.3e-4` at `M=1020,N=5000` sits well inside that tolerance; the test would pass for that case.

### Reframe (Phase 4.5)

This investigation reframes from "find and fix the bug" to "characterize the gap between user expectation and maintainer policy":

- The reporter expects flat/view to be interchangeable to bitwise precision because non-Reactant Julia `mul!` happens to give identical results.
- Reactant's policy, as expressed in the existing test, is FP32 tolerance, not bitwise match.
- The actual delta is one or two ULPs accumulated across the reduction — well inside the existing test's tolerance.
- A "fix" that forces materialization at the jit boundary would cost a `M*K*sizeof(ComplexF32)` copy per call to recover bitwise equivalence — that is a perf regression maintainers may not want to absorb for a policy they didn't sign up for.

This is a **maintainer-decision** edge, not a substrate-fix edge. The structural artifact is this graph plus a tissue comment that hands findings back to the issue without a PR.

### New frontier edges

1. (Open) Confirm at runtime that the two HLO graphs actually select different GEMM kernels. Would require a Julia/Reactant env not available this round; `Reactant.@code_hlo f!(out, c_view, …)` vs `Reactant.@code_hlo f!(out, c_flat, …)` would expose this in one command.
2. (Open) Confirm the existing test at `test/integration/linear_algebra.jl:86-105` passes with current main on the reporter's sizes. If yes, evidence H3 stands.
3. (Open, deferred to maintainer) Decide whether Reactant should canonicalize dense rank-preserving views at the jit boundary, or document the existing tolerance policy more visibly.

## Halt

Halting before Phase 8. The surviving hypothesis is a policy/expectation reframe, not a code change. Output is this graph plus an optional tissue comment on the issue summarizing the structural finding and the existing-tolerance signal.

## 2026-05-18 Round 3 — independent re-read, same verdict

Re-traced `overloaded_mul!` (`src/stdlibs/LinearAlgebra.jl:278-335`) and `materialize_traced_array(::SubArray)` (`src/TracedUtils.jl:63-65`) on hydrated worktree (head `954d31c`). No new structural finding. Re-confirmed `test/integration/linear_algebra.jl:92-106` ships view-`mul!` under `atol=1e-3, rtol=1e-2` — an order of magnitude looser than the reporter's worst-case `1.3e-4` at `M=1020, N=5000`. Diagnosis unchanged: H1 structural, H3 framing, tissue not PR. No additional perturbations possible without Julia+Enzyme+MLIR+XLA stack.

## Provenance

Not yet available. Mandatory provenance checks require a local git worktree:

- `git blame` on view/SubArray lowering and `mul!`/BLAS lowering.
- Search upstream issues/PRs for `SubArray`, `view`, `mul!`, `HLO`, `slice transpose reshape`, and `GEMM`.
- Check for existing canonicalization passes that should already handle dense views.

## Blocked Experiment Log

- `sweep project-info EnzymeAD/Reactant.jl`: worktree missing; QA env would be Docker `sweep-tester:latest`.
- `gh issue view 2904 --repo EnzymeAD/Reactant.jl --json ...`: failed because shell could not connect to `api.github.com`.
- `git ls-remote https://github.com/EnzymeAD/Reactant.jl.git HEAD`: failed because shell could not resolve `github.com`.
- Local package search under `/Users/junekim/.julia`: no `Reactant*` directory found.
- Docker tester image check: no `sweep-tester:latest` image reported.

